import { IssueCountFilterPipe } from './issue-count-filter.pipe';

describe('IssueCountFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IssueCountFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
